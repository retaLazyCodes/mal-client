version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/app:/app/app
      - ./backend/media:/app/media
      - ./backend/data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
